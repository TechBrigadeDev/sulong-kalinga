import { ScrollView } from "tamagui";

import { Calendar } from "react-native-calendars";
const Home = () => {
    return (
       <ScrollView>

        <Calendar/>
       </ScrollView>
    );
}

export default Home;