import { ChatItem } from './list.types';

// TODO: Implement actual API calls
export const getChats = async (): Promise<ChatItem[]> => {
    return [];
};

export const getPinnedChats = async (): Promise<ChatItem[]> => {
    return [];
};
